# looks like it's not loaded. just keeping to put them in .zshrc
#plugins=(ant autojump bundler cpanm django gem git github heroku mercurial pip python rails rails3 redis-cli ruby rvm ssh-agent screen sublime svn vundle)
